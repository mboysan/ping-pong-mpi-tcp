package protocol.commands.ping;

import protocol.commands.NetworkCommand;

public class EndAll_NC extends NetworkCommand{
    @Override
    public String toString() {
        return "EndAll_NC{} " + super.toString();
    }
}
